/target

# Installer build output (produced by installer/storage-analyzer.iss)
/dist

# Local tooling state, not project source
/.claude

# OS cruft
Thumbs.db
desktop.ini
v2/
# Editor state
/.vscode
/.idea

# Report files the analyzer itself writes when run from the repo root
storage-analyzer-export.csv
storage-analyzer-export.json
